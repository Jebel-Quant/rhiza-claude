{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/hook_bash_guard.py\"",
            "timeout": 10,
            "statusMessage": "Checking the command against the rhiza guards"
          }
        ]
      }
    ]
  }
}
