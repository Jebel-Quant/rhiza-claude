# Per-machine Claude Code overrides — never committed
settings.local.json

# OS / editor cruft
.DS_Store

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/

# MkDocs book build + generated dashboard
_book/
.cache/

# Test reports from `make test` (coverage.xml + browsable HTML), and the copy
# `make book` stages into the site — measured on every run, never committed
_tests/
docs/reports/

# Paper PDF staged into the docs site by `make paper` — built, not committed
docs/paper/
